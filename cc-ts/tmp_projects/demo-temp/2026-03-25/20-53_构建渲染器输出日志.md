# 构建渲染器输出日志



> cc-ts@0.1.0 build:renderer
> mkdir -p dist/renderer && cp src/renderer/index.html dist/renderer/index.html && cp src/renderer/panel.css dist/renderer/panel.css && esbuild src/renderer/app.tsx --bundle --platform=browser --format=iife --target=chrome120 --outfile=dist/renderer/app.js

